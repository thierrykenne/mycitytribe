<h1>Destination list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Countryid</th>
            <th>Name</th>
            <th>Type</th>
            <th>Parentid</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('dest_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.countryId }}</td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.type }}</td>
            <td>{{ entity.parentId }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('dest_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('dest_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('dest_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
