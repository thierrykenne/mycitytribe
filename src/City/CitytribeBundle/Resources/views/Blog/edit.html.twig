
{% extends "CitytribeBundle::default.html.twig" %}
{% block middle %}
<div class="form-msg">
	<p><h2>Edit Subject</h2></p>

	<p> 
		<form action="{{ path('message_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
	    <p>
	        <button class="btn btn-success" type="submit">Edit</button>
	    </p>
	</form>

	<ul class="record_actions">
	    <li>
	        <a href="{{ path('Cityblog_home') }}">
	            Back to the list
	        </a>
	    </li>
	</ul></p>
</div>

{% endblock %}

